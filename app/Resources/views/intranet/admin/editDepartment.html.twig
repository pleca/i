{% extends 'intranet/admin/admin.html.twig' %}
{% block adminBlock %}

    {#<?php#}
    {#require_once('controllers/singleNewsController.php');#}
    {#$array = getSingleNews($id, $db_user, $db_password);#}
    {#?>#}
        <div class="well well-sm"><span class="path"><a href="{{ path('Strona Główna') }}">Strona Główna</a> <a href="{{ path('Struktura') }}">/ Struktura</a> / {{ department.departmentName }} (edycja)</span></div>
        <div class="row text-center news-row">
            <div class="col-xs-12">
                <div class="news-title">
                    <h1>{{ department.departmentName }}</h1>
                </div>
            </div>
        </div>
        <div class="form-group edit-div">
            <form id="edit_division_form" role="form" action="{{ path('edytuj departament', {'departmentName': department.departmentName })|raw }}" method="post">`
                <div class="panel-body">
                    <fieldset>
                        <div class="form-group">
                            <label for="name" class="control-label">Nazwa</label>
                            <input type="text" class="form-control" id="name" name="department" placeholder="Nazwa departamentu" value="{{ department.departmentName }}">
                        </div>
                        <div class="form-group">
                            <label for="director" class="control-label">Dyrektor</label>
                            <select name="director" class="form-control" id="director">
                                <option value="{{ directorId }}" selected>{{ director }}</option>
                                {% for el in employees %}
                                    <option value="{{ el.userId }}">{{ el.userName }}</option>
                                {% endfor %}
                            </select>
                        </div>
                    </fieldset>
                </div>
                <div class=" clearfix text-center"><button type="submit" class="btn btn-default">Zapisz</button></div>
            </form>
        </div>
{% endblock %}